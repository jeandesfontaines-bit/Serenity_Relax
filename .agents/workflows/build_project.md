---
description: Build the AuraFlow Wellness Next.js project
---

# Build Project

// turbo-all

## Steps

1. Install dependencies:
```bash
npm install
```

2. Run the production build:
```bash
npm run build
```

The build output will be in the `.next` directory. The app uses Next.js 15 with Turbopack.
