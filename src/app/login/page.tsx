'use client';

import LoginPanel from '@/components/auth/LoginPanel';

export default function LoginPage() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-[var(--off-white)] p-8">
      <LoginPanel />
    </div>
  );
}
